# 基于 qt5.15.2 的串口助手
使用静态编译。主要功能有串口的收发，串口基本设置。串口读取方式分为：常规、总字节数、数据位。发送方式有字符发送，十六进制发送，以及定时发送。
普通接收为：qt串口类QSerialPort::readyRead 信号到来时调用readall()函数一次性读取，串口收发不稳定的情况下，数据接收可能不全。按总字节数接收，当串口接收到规定的总字节数后发送接收信号给UI。按位接收，首先接收到规定位，然后根据规定位的字节数，继续接收；接收到字节数：规定位+规定位字节数。
![界面](imgs/1.png)
## 跨线程发送暂未实现